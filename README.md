# Rivo Android APK

This is a ready-to-build Android WebView wrapper for the Rivo web app.

The GitHub Actions workflow installs Gradle automatically, so a `gradlew` file is not required.

Build locally:
    gradle assembleDebug

APK:
    app/build/outputs/apk/debug/app-debug.apk
